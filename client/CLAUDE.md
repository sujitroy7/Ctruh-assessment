@AGENTS.md

# Design System

This project has a design system. Read `FRONTEND.md` before writing any UI code.

## When to read FRONTEND.md

Read it when the task touches `components/` or `app/` (pages, layouts, JSX).

## When to skip FRONTEND.md

Skip it for `lib/`, `types/`, API route handlers, and any non-UI logic.
